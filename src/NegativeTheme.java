import java.awt.*;

public class NegativeTheme extends Theme {
    public NegativeTheme() {
        background = Color.WHITE;
        snakeHead = Color.BLACK;
        snakeBody = new Color(60, 60, 60);
        foodBody = Color.BLACK;
        foodEyes = Color.RED;
        foodNose = Color.WHITE;
        eyeColor = Color.WHITE;
        borderColor = Color.GRAY;
    }
}
